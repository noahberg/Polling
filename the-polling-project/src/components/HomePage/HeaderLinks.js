import styled from "styled-components";

export default styled.div`
    font-family: ${props => props.theme.font};
    cursor: pointer;
    font-weight: 500;
    font-size: 0.8rem;
    color: white;
    align-self: center;
    justify-self: center;
`;