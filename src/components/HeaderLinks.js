import styled from "styled-components";

export default styled.div`
    font-family: ${props => props.theme.font};
    font-weight: 500;
    font-size: 1.5vw;
    color: white;
    align-self: center;
    justify-self: center;
`;